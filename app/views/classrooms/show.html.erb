Classroom Info
<p>
<%= @classroom.course_name %>
<%= @classroom.semester %>
</p>

We now want to call out the all the students in the class:
<% @students.each do |student| %>
<%= render partial: 'students/student', locals: {student: student} %>
<% end %>
